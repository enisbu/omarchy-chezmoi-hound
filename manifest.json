{
  "schemaVersion": 1,
  "id": "enisbu.chezmoi-hound",
  "name": "Chezmoi Hound",
  "version": "1.2.0-enisbu.1",
  "author": "Enis Budancamanak",
  "license": "MIT",
  "homepage": "https://github.com/enisbu/omarchy-chezmoi-hound",
  "description": "A bar count of dotfile drift that hunts it down. It adds up what this machine has edited and the source has not captured, what the source repo has not committed, and what the remote has not seen - then the panel names the items and offers to push them or capture and commit them.",
  "kinds": [
    "bar-widget"
  ],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Chezmoi Hound",
    "description": "Dotfile drift across $HOME, the source repo and its remote, with push and capture-and-commit actions.",
    "category": "System",
    "aliases": [
      "dotfiles",
      "chezmoi",
      "drift"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "sourceDir": "",
      "checkSeconds": 300,
      "whenClean": "Hide",
      "aiCommand": ""
    },
    "schema": [
      {
        "key": "sourceDir",
        "type": "string",
        "label": "chezmoi source directory",
        "defaultValue": "",
        "description": "Leave empty to use chezmoi's own configured source. Set it when the source is a tree you normally pass to `chezmoi --source`, such as ~/Projects/dotfiles."
      },
      {
        "key": "aiCommand",
        "type": "string",
        "label": "AI command for commit messages",
        "defaultValue": "",
        "description": "Leave empty to use this machine's default coding agent, the one `omarchy default agent` reports. Supported for suggestions: hermes, claude, codex, gemini, opencode. Name one to force it instead, such as `codex`. Only used when the Suggest button is pressed."
      },
      {
        "key": "checkSeconds",
        "type": "integer",
        "label": "Re-check every (seconds)",
        "min": 60,
        "max": 3600,
        "step": 30,
        "defaultValue": 300
      },
      {
        "key": "whenClean",
        "type": "enum",
        "label": "When everything is in sync",
        "options": [
          "Hide",
          "Show"
        ],
        "defaultValue": "Hide",
        "description": "Hide keeps the bar free of a permanent zero."
      }
    ]
  }
}
